const path = require('path');
const CopyWebpackPlugin = require("copy-webpack-plugin");
module.exports = {
  mode: 'production',
  context: path.resolve(__dirname),
  entry: {},
  plugins: [
    new CopyWebpackPlugin({
      patterns: [
        {
          from: path.resolve(__dirname, 'src/api/function/register/Dockerfile'),
          to: path.resolve(__dirname, 'lib/api/function/register')
        },
        {
          from: path.resolve(__dirname, 'src/api/function/unregister/Dockerfile'),
          to: path.resolve(__dirname, 'lib/api/function/unregister')
        },
      ]
    }),
  ],
};